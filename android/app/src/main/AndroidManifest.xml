<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.RECORD_AUDIO" />

    <application
        android:allowBackup="false"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.KlariVision">
        <!-- Boyut değişimi (döndürme, bölünmüş ekran, pencere boyutu, ekran
             yoğunluğu) etkinliği yeniden yaratmamalı: yeniden yaratma açık
             çalışmayı kapatıp Ana Sayfa'ya dönüyordu. 03-responsive-contract.md
             "Kalıcılık kuralları"; Swift tarafı da durumu korur. -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:configChanges="orientation|screenSize|smallestScreenSize|screenLayout|density|keyboardHidden|uiMode"
            android:label="@string/app_name"
            android:theme="@style/Theme.KlariVision">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>
